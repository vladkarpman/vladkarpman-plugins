# Core dependencies (Python 3.9+)
Pillow>=10.0.0
imagehash>=4.3.0
numpy>=1.24.0

# scrcpy-helper dependencies (Python 3.11+ required, installed in venv)
# These are installed in scripts/scrcpy_helper/.venv/
# - mysc (MYScrcpy) - for scrcpy 3.x support
# - adbutils - for device management
# - av - for video processing

# To install scrcpy-helper venv:
# cd scripts/scrcpy_helper
# python3.12 -m venv .venv
# .venv/bin/pip install mysc Pillow imagehash numpy adbutils
